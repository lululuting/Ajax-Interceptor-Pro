# 使用说明

## 这是什么

`Ajax Interceptor Pro` 是一个 Chrome 接口拦截插件，可以在前端开发、联调和调试时，直接拦截请求并返回你自定义的响应内容。

它基于 Ajax Interceptor 的思路继续扩展，加入了分组管理、命中计数、导入导出和更完整的可视化界面。

## 安装

1. 执行 `npm install`
2. 执行 `npm run build`
3. 打开 Chrome，进入 `chrome://extensions/`
4. 打开右上角“开发者模式”
5. 选择“加载已解压的扩展程序”
6. 选择项目里的 `dist/` 目录

## 使用流程

### 1. 新建分组

点击左侧“新建分组”，给规则分类。

默认会保留一个 `未分组`，你也可以自己继续加业务分组，比如：
- 用户模块
- 订单模块
- 支付模块

### 2. 添加规则

点击“添加规则”，填写：
- 规则名称
- 请求方法
- URL 匹配模式
- 所属分组
- 返回数据
- 状态码

规则命中后，插件会直接返回你配置的响应内容。

### 3. 查看命中情况

每条规则右侧都有命中计数。命中后会立即更新，方便确认请求是否真的走到了这条规则。

### 4. 导入导出配置

设置里支持导出当前配置，也支持导入已有规则：
- `覆盖`：用导入内容替换当前规则
- `合并`：按分组和规则合并，尽量避免重复

## 两种打开方式

### 弹窗模式

点击浏览器扩展图标，直接打开插件界面。

开启「全局开关」后，会对**所有已打开的普通网页**生效（按规则匹配请求）。

### DevTools 模式

按 `F12` 打开开发者工具，在 `Ajax拦截` 面板里使用。

只对**当前被调试的那一个标签页**生效，且需要面板保持连接、当前页开关打开。

## 注入范围与影响（请必读）

扩展会在匹配的网页里注入脚本（`<all_urls>`，含 iframe，页面加载最早阶段）：

| 状态 | 实际行为 | 对页面的影响 |
| --- | --- | --- |
| 任意模式，开关关闭 | 仍会挂上 `fetch` / `XHR` 包装，但请求**同步直通**，不改响应 | 一般很小；个别会检测“是否原生 API”的 SDK/监控脚本仍可能受影响 |
| 弹窗模式 + 全局开关开启 | 所有页面的匹配请求都可能被拦截/改写 | 影响面最大，不用时请关掉全局开关 |
| DevTools 模式 + 当前页开关开启 | 仅当前调试页的匹配请求被拦截/改写 | 影响面较小，但仍会改该页请求 |

建议：

- 日常不用时保持开关关闭
- 联调结束后关掉开关，或暂时禁用本扩展
- 若某业务页 SDK/监控异常，先关开关并刷新该页验证

## 适合用来做什么

- 本地没有后端时先模拟接口
- 接口字段调试
- 异常返回模拟
- 固定响应复现 bug
- 前端联调前自测

## 注意事项

- URL 匹配支持通配符
- 如果规则没命中，先检查请求方法和 URL 是否一致
- 建议调试时关闭浏览器缓存
- 弹窗模式与 DevTools 模式的拦截开关均默认关闭；需要拦截时请手动开启
- 关闭开关后请求会同步直通；开启后一般无需刷新即可生效。若页面在开启前已缓存原生请求方法，可刷新一次页面
