{
  "name": "ajax-interceptor-pro",
  "version": "2.1.0",
  "description": "基于 Ajax Interceptor 演进的 Chrome 接口拦截插件，支持分组管理、导入导出和命中统计。",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "dev": "vite build --watch",
    "build": "vite build && node scripts/post-build.js"
  },
  "type": "module",
  "license": "ISC",
  "dependencies": {
    "antd": "^6.3.5",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.1",
    "vite": "^8.0.3"
  }
}
